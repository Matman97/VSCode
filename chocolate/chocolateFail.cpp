#include <iostream>

using namespace std;

int main(){

    int vasos, cucharas, personas;

    cout << "Ingrese el numero de vasos: ";
    cin >> vasos;

    cout << "Ingrese el numero de cucharas: ";
    cin >> cucharas;

    cout << "Ingrese numero de personas: ";
    cin >> personas;

   
    if (personas < 5){ //Posicion del if.
//No es necerario usar la variable de persona para la condicion del if .
        cout << "Los vasos alcanzan ";

    if (personas < 5) //No cambia el resultado la posicion del if.

        cout << "Las cucharas alcanzan ";

        }

     else; 
        cout << "Los vasos no alcanzan ";
        
        cout << "Las cucharas no alcanzan ";

         }
         // y=& , o=|